# SDD ledger — plan: docs/superpowers/plans/2026-09-19-pepe-habla-phases-0-1.md

Spec: docs/superpowers/specs/2026-09-19-pepe-habla-design.md (read)
Branch: phases-0-1, off main @073d177. Baseline: 49 tests pass, typecheck clean.

## Pre-flight conflict scan

| # | Checked | Finding |
|---|---|---|
| 1 | T1 creates `packages/core/src/index.ts`; T3, T4, T9 modify it | clean — each appends one export line, no overlap |
| 2 | T1 moves `apps/assets` → `apps/app/assets`; T2 writes into it | clean — ordering correct, T2 depends on T1 |
| 3 | T2 produces `manifest.json`; T6 imports it | clean — `{poses, vocab}` shape matches both sides |
| 4 | T2's `VOCAB` word ids vs `data/seed/*.json` | **DEFECT** — 3 of 4 ids absent from seed data |
| 5 | T2 writes `vocab-<id>.png`; T6's `VOCAB_ART` keys | clean — same filename convention |
| 6 | T4 widens `Direction`; T10/T11 switch on it | clean — T4 precedes both |
| 7 | T3 defines `AnswerRecord`; T6 `recordAnswers` consumes it | clean — one definition, unchanged |
| 8 | T5 produces theme/PressableCard/Screen; T9/T10 consume | clean — T5 precedes both |
| 9 | T6 produces `POSES`/`VOCAB_ART`; T7/T10 consume | clean — `PoseName` flows through |
| 10 | T5 creates `app/index.tsx`; T9 deletes it | clean — deletion is intended, stated in T9 |
| 11 | T10 creates `session.tsx`; T11 modifies it | clean — T11 edits are anchored to T10's exact text |
| 12 | T11 uses `radius`, `roundScore`, `bumpStreak` | clean — T10 imports `radius`; T11 step 1 adds the rest |
| 13 | T11 summary uses `<Screen>` | clean — 1 open, 1 close, no stale SafeAreaView |
| 14 | Every task: own tests vs own code, files created vs later touched | clean except row 15 |
| 15 | T1 step 2 shell block | **DEFECT** — a no-op `sed` against `/dev/null`, leftover |

Ruling: missing vocab ids — added `el-taco` (tier 1), `la-guitarra` and `el-sombrero` (tier 2) to the seed rather than dropping three of the four picture cards. *el taco* absent from a Mexican Spanish word list is a gap on its own merits, and picture cards were approved as a feature so they need art-backed words. Seed is now 384 words. Cost if wrong: three extra words in the list, trivially removable.
Ruling: the no-op sed line — deleted from the plan. It operated on `/dev/null` and did nothing; the real instruction is the hand-edit that follows it. Cost if wrong: none, it was dead.

## Tasks

Task 1: review — spec ❌ (missing `packages/core/tsconfig.json`, a named deliverable the plan never gives content for), quality Approved.
Task 1: Ruling: create the file rather than strike it from the plan. The plan's Files block names it, the root tsconfig happens to cover core today so nothing breaks, but a package that cannot be typechecked on its own is exactly what makes a future web build expensive — and web reuse is the reason core is pure. Content supplied by me since the plan omitted it. Cost if wrong: one redundant config file.
Task 1: Ruling: the implementer's out-of-scope edit to `.claude/commands/practice.md` stands. The move renamed `src/cli.ts`, so leaving it would have shipped a broken `/practice` command; fixing a caller you just broke is part of the move, not scope creep. Cost if wrong: three path strings in a doc.
Task 1: minor (deferred): `noNodeImports.test.ts` regex matches only single-quoted specifiers — a double-quoted or dynamic import would slip past.
Task 1: minor (deferred): README still documents the old `src/core` / `src/storage` / `src/cli.ts` paths.
Task 1: fix round 1/5 (1 addressed, 0 open — packages/core/tsconfig.json created, standalone typecheck clean; commits 8e4d1d7..2dd1194)
Task 1: complete (commits 498325b..2dd1194, review clean)

Task 2: review — spec ✅, quality: 1 Important (the `_split` helper is never called; reviewer instrumented it and measured zero invocations across all three sheets, so the 53→67 gain came entirely from the dilation change and the report's rationale is false), plus minors.
Task 2: Ruling: delete `_split` rather than keep it with a corrected comment. It is ~35 lines of never-executed, untested code carrying an unhandled three-way-merge case, in a developer tool that has already done its job. Removal is provably safe — the reviewer measured zero calls, so re-running must reproduce the same 67 sprites byte-for-byte, which is the verification. If a future sheet does merge, the process already mandates eyeballing the contact sheet, and splitting can be written then against a real failure instead of an imagined one. Cost if wrong: re-adding a helper we have in git history.
Task 2: minor (deferred): `sprite_manifest.py` copies files before its missing-check gate, so a failed run can leave orphaned art in the output directory.
Task 2: minor (deferred): `_split`'s successor, if ever written, must handle blobs merging three drawings, not just two.
Task 2: note: controller housekeeping commit 1a87260 untracked a stray `.pyc` and added `.superpowers/`, `__pycache__/`, `*.pyc` to .gitignore — not part of the task.
Task 2: fix round 1/5 (1 addressed, 0 open — `_split` deleted, re-extraction byte-identical at 67 files, masking kept, docstring and report corrected; commits 1a87260..6cc7414)
Task 2: complete (commits 2dd1194..6cc7414, review clean)

Task 3: review — spec ✅, quality: 1 Important (`anotherRound` is applied from any phase: dispatched mid-repair it silently drops the pending repair queue, and dispatched after `finish` it resurrects a finished session).
Task 3: Ruling: add the guard rather than rely on the UI never misdispatching. Tasks 10 and 11 drive this reducer directly, and the failure is silent — the learner's missed words vanish with no error, which is exactly the class of bug this machine was pulled into core to prevent. Guarding `anotherRound` to `summary` also makes `finished` genuinely terminal, so one condition fixes both halves. Cost if wrong: a rejected transition the UI would have to notice, caught immediately by the new tests.
Task 3: fix round 1/5 (1 addressed, 0 open — `anotherRound` guarded to the summary phase; re-reviewer removed the guard and confirmed both new tests fail without it, so neither is vacuous; commits d0045a3..62c549a)
Task 3: complete (commits 6cc7414..62c549a, review clean)

Task 4: review — spec ✅, quality Approved. Reviewer diffed all three pre-existing tests against 62c549a and confirmed none were weakened: the one loosened assertion (`dirs.size === 2` → `has both`) was forced by the widening and is as strong as remains true.
Task 4: minor (deferred): for rounds of 4 words or fewer the 10/20/30% quotas all round to zero, so short rounds collapse to `es->en` only. Does not affect repair rounds, which re-ask existing Question objects rather than rebuilding them.
Task 4: complete (commits 62c549a..8f1ea59, review clean)

Task 5: review — spec ✅ (both deviations justified: `@expo/ui` install, and wrapping the temp screen in `<Screen>` so step 11's centring check was not vacuous), quality: 2 Important.
Task 5: Ruling: fix the typecheck gap now rather than defer. I reproduced it — `tsc --noEmit -p apps/app` fails with TS5097, and the root `typecheck` script's `include` covers only `packages/core` and `tools`, so the app's TypeScript is checked by nothing at all. Six remaining tasks write app code; leaving it means type errors accumulate invisibly until something breaks at runtime on a device, which is the most expensive place to find them. Cost if wrong: a stricter check that might surface pre-existing errors — which is the point.
Task 5: Ruling: the `@expo/ui` code change stands, but the report's stated root cause is wrong and must be corrected. The reviewer checked `node_modules/expo-router/package.json` and found `@expo/ui` in regular `dependencies`, not undeclared peers, so the install was an ordering artifact rather than a gap in expo-router. Same defect class as Task 2: harmless code, false explanation, which misleads the next reader. Cost if wrong: none, the dependency is correctly versioned either way.
Task 5: verified by me — iOS screenshot shows `@pepe/core` values rendering in the Expo bundle (todayISO, INTERVALS, mulberry32), both fonts loaded, hard shadow slab correct.
Task 5: fix round 1/5 (2 addressed, 0 open — apps/app now typechecked and the root script chains both invocations; re-reviewer injected a deliberate TS2322 and confirmed the app invocation catches it; `@expo/ui` claim corrected; commits 78f1ac0..e885fa2)
Task 5: complete (commits 8f1ea59..e885fa2, review clean)
Task 5: minor (deferred): `metro.config.js` sets `disableHierarchicalLookup = true` (from the brief). Any future dependency npm nests rather than hoists will not resolve.

Task 6: review — spec ✅, quality Approved. Reviewer verified the word-id → manifest filename → VOCAB_ART key → file chain end to end, confirmed `recordAnswers` never mutates the bundled seed object, and confirmed a word without art gets no `sprite` key at all rather than `undefined` (which would still be an own property and could confuse the picture-question gate).
Task 6: minor (deferred, FLAG FOR FINAL REVIEW): `loadProgress` catches a JSON parse failure and silently returns the bundled 10-word seed. A learner months in whose stored blob gets truncated would lose everything with no error and no trace. Deferred rather than fixed now because phase 3 migrates this storage format for the SM-2 scheduler and that is the natural moment to harden it — but the minimum fix is to preserve the unparseable blob under a backup key instead of discarding it. This is the deferred minor I would most want addressed.
Task 6: note: the brief's prose "Produces" block mentions a `SPRITES` export that its own code block never defines. Implementer correctly followed the code. Plan defect, no impact.
Task 6: complete (commits e885fa2..1a65e31, review clean)

Task 7: review — spec ✅ (timings verbatim), quality: 1 Important (no unmount cleanup for the infinite animations), 1 Minor (hop's squash peaks 50ms before touchdown). Both inherited from the brief, not implementer error.
Task 7: Ruling: the reviewer's central claim — that `withSequence`/`withRepeat` may not visibly animate at all on Expo Go + SDK 57 + Reanimated 4.5.1 — is WRONG, and I disproved it myself rather than dispatch a third agent. I slowed `breathe` (which is `withRepeat(withSequence(withTiming))`) to a 60s cycle at 300px amplitude, ran it on iPhone 16 Pro, and captured two frames 22s apart: Pepe is lifted clear off the top of the screen in one and back at rest in the other, with a 681x660px changed region. The composed helpers animate correctly. Both the implementer's and the reviewer's failures were methodological — sub-second durations against multi-second tool round trips. Cost if wrong: none, the evidence is two screenshots.
Task 7: Ruling: fix both findings. The unmount cleanup is trivially correct. The squash timing is worth correcting rather than deferring because it is the difference between a dog landing and a sticker sliding — the exact quality the squash exists to produce — and it is a three-number change.
Task 7: fix round 1/5 (2 addressed, 0 open — unmount cleanup added unconditionally, hop squash retimed so its minimum lands at 300ms with touchdown, both sequences still 620ms; commits 731dbc4..fb7419e)
Task 7: note: the re-reviewer labelled lift's 620ms total as "touchdown" (touchdown is 140+160=300ms). Sloppy labelling, correct conclusion; I verified the arithmetic from the diff myself.
Task 7: complete (commits 1a65e31..fb7419e, review clean)

Task 8: review — spec ✅ (feedback.ts byte-for-byte the brief; `app.json`/`package-lock.json` inclusion judged correct, as `expo install` legitimately registers the expo-audio config plugin), quality: 1 Important, 2 Minor.
Task 8: Ruling: fix the `attempt()` gap. It only catches a promise the callback returns, and two call sites use block bodies that discard one — `player.seekTo(0)` and `Speech.stop()`, both confirmed `Promise<void>` in the installed typings. Brief defect, mine. Chaining them rather than merely returning them also fixes two real races the reviewer logged as separate minors: play now waits for the seek so a rapid re-fire restarts cleanly, and speak now waits for the stop so a new utterance is not cancelled by the in-flight one. Three findings, one change. Cost if wrong: a few ms of latency before each cue.
Task 8: minor (deferred): `players` are never `.remove()`d; a Fast Refresh re-running `prepareAudio()` orphans native player objects. Development-only.
Task 8: minor (deferred): `attempt()` swallowing everything means a genuinely broken audio pipeline is undiscoverable. Defensible for a feedback module, but worth a dev-only log later.
Task 8: HANDOFF: audibility, haptics, the es-MX accent and silent-switch behaviour cannot be verified without a physical phone. These need the user's own device at the end of the plan.
Task 8: fix round 1/5 (1 addressed, 0 open — both call sites chained; re-reviewer audited all five remaining `attempt(` sites and confirmed `createAudioPlayer` is synchronous so the one remaining block body is correct; commits 85f5af1..e66bfed)
Task 8: complete (commits fb7419e..e66bfed, review clean)

Task 9: Ruling: the report cited `screenshots/ios-home.png` as evidence for the home screen, but that file is timestamped 00:45 and belongs to Task 5 — it shows the core-proof card, not a home screen. No new screenshot was taken. I ran the app myself and captured `screenshots/ios-home-verified.png`: the screen is real and the implementer's written description was accurate in every particular, and `POR REPASAR: 1` does match `node tools/cli.ts stats` → `Due today: 1`. So this is a stale-evidence citation, not a fabricated observation. Recording it because a review process that cannot trust a report's evidence pointers is not a review process. Cost if wrong: none, I have the screenshot.
Task 9: review — spec ✅ (the `ColorValue` deviation verified necessary against the installed react-navigation types), quality Approved, minors only. Reviewer confirmed `bumpStreak` fails safe on a negative gap (clock rolled back resets to 1, cannot be exploited to inflate a streak).
Task 9: minor (deferred): `known` is `box >= 4`, not the spec's honest "right in both directions with reps >= 3" — the `Progress` type has no per-direction counters until phase 3. The home screen will therefore read CONOCIDAS 0 until words reach box 4. Brief-specified, and the honest definition is a phase-3 deliverable.
Task 9: minor (deferred): `Bunting` uses a fixed 350px default width rather than measuring the device; may clip slightly below ~390pt.
Task 9: minor (deferred): the home screen's `useFocusEffect` has no cancellation flag; benign under expo-router's mounted-but-unfocused tabs.
Task 9: complete (commits e66bfed..ac93ed2, review clean)

PLAN CHANGE (user-directed, after hearing the web build speak Spanish with an English voice): added Task 9b — ship a Paulina (es-MX) recording for every word as ~8KB AAC, with expo-speech behind it. Fallback chain is: bundled recording -> device speech with an explicitly chosen es-MX voice -> nearest Spanish -> silence. The last step is deliberate: a device with no Spanish voice reads Spanish with an English mouth, and teaching a wrong pronunciation is worse than teaching none. `speak()` now takes a word object rather than a string; the plan's Task 10/11 call sites were updated and both briefs restaged.
Task 9b: Ruling: reverted `optionMeaning` and its 5 tests, which an interrupted Task 10 agent had written and which my own `git add -A` on the plan-change commit (8c879ea) then committed under an unrelated message. Reverting rather than keeping, because the code never went through the red step — nothing demonstrates those tests fail without the function, and Task 3's re-review already showed vacuous tests are a live risk here. Task 10 writes it properly. Cost if wrong: one agent re-deriving ~30 lines it has the brief for. Test count back to 76.
Task 9b: note: my `git add -A` is what let unreviewed work into a commit. Narrower adds from here.
Task 9b: Ruling: also reverted `apps/app/app/session.tsx` (193 lines) and `components/OptionButton.tsx`, both Task 10 deliverables written by an interrupted agent and committed by my own `git add -A`. session.tsx still had `buildRound(db.progress, todayISO(), 25)` — the round number hardcoded to 25 from someone debugging — which is what unfinished work looks like. Untracked 4 .DS_Store files and gitignored them. Cost if wrong: Task 10 re-derives files it has a complete brief for.
Task 9b: Ruling: KEPT the app icon pipeline (`tools/make_icon.py`, regenerated icon/favicon/android icons) that also arrived unreviewed, because the user added `images/originals/pepe-hablas-icon*.jpeg` deliberately and the output is correct and on-palette. Flagged to the user and to the final review as unreviewed, out-of-plan work. Cost if wrong: an icon that can be regenerated or reverted in one command.
Task 9b: note: `apps/app/AGENTS.md` instructs reading https://docs.expo.dev/versions/v57.0.0/ before writing app code. Carry this into every remaining apps/app dispatch.
Task 9b: review — spec ✅ (reviewer re-ran make_speech.py and got a zero diff, proving speech.ts is genuine generated output; 384 entries, every require resolves), quality: 1 Important.
Task 9b: Ruling: fix the startup race. Before `prepareSpeech()` resolves, `voicesChecked` is false, so `speak()`'s guard does not fire and it calls `Speech.speak` with a language hint and no explicit voice — precisely the pattern that produced the English-mouth bug the task exists to fix. Latent today because every seed word has a recording, but Tasks 10 and 11 consume this contract and phase 3 will add words faster than renders. `speak` must await the voice check rather than race it, and `canSpeak` must require its word argument so it cannot optimistically report speakable before the check lands. Cost if wrong: a speaker button inert for the few ms before startup completes.
Task 9b: fix round 1/5 (1 addressed, 0 open — speak now awaits the voice check and passes an explicit voice; implementer verified on device at the true race window: canSpeak false before resolution, deferred speech after, Paulina identifier used; commits 7826966..42514f5)
Task 9b: complete (commits 8c879ea..42514f5, review clean)

COORDINATION: a separate Claude Desktop session ("App icon configuration", started 4h ago, still busy) is committing to this same branch — 5b4c35d (iOS dark/tinted icon variants) and 15d5725 (a Welcome component, plus `pickOne` added to packages/core with three tests). I inspected both: the core addition is pure, tested at its edges, and the purity guard still passes, so nothing is damaged. Not reverting another session's work. The final whole-branch review diffs from merge-base and will cover it. Flagged to the user — the real cost so far was Fast Refresh resets disrupting Task 10's on-device verification.
Task 10: implementer flag — only 4 of 384 words carry sprite art, so a natural ten-question round can contain no picture question at all (today's did not). Not a Task 10 defect; thin art coverage. 67 sprites were extracted and only 4 mapped, so this is cheap to widen.

Task 10: review — spec ✅ with genuine TDD evidence (reviewer confirmed the red step showed a real missing-export failure, unlike the reverted attempt), quality: 2 Important, 1 Minor. Both Importants are brief defects of mine.
Task 10: Ruling: fix the auto-speak double-fire. The effect's dep `state?.phase === 'asking'` flips true→false on answering, so it re-fires and speaks again during feedback — on a wrong answer it collides with onAnswer's own speak() and the shared wordPlayer gets replace()+play() twice. Guard the body on the phase rather than relying on the dep array. Cost if wrong: a listening question that does not auto-speak, immediately obvious.
Task 10: Ruling: fix round-to-round repetition. `selectDaily` returns every due word when fewer than ten are due, regardless of seed, so "¿Otra ronda?" can serve the identical ten words — contradicting buildRound's own comment. Exclude ids already answered this session and let selectDaily top up from unseen words. Task 11 depends on this being right, so it is load-bearing rather than deferrable. Cost if wrong: a second round that introduces new words sooner than intended.
Task 10: minor (deferred): the `optionMeaning` test "returns null for an option that is not in the pool" passes against a return-null-always mutant. Boundary test, low discriminating power; worth pairing with a positive assertion when that file is next touched.
COORDINATION: user directed me to stop the parallel session. Stopped "App icon configuration" (local_70742128). A second session "Android upload capability" shares the same cwd but is idle. Its commits (5b4c35d, 15d5725) stay on the branch — inspected, sound, and the final whole-branch review covers them.
Task 10: fix round 1/5 (2 addressed, 0 open — auto-speak guarded on phase inside the effect body, buildRound given an exclude set while distractors stay drawn from the full WORDS; implementer proved single-fire by streaming simulator logs and proved round disjointness with real id lists; commits 66ac700..bd04a4b)
Task 10: complete (commits 9023990..bd04a4b, review clean)
Task 11: review — spec ✅, quality Approved, no findings. Reviewer audited the repair-never-records rule end to end in code (not the report): persistence filters to the current round, there is exactly one writer, and core's reducer guarantee is not circumvented. Also confirmed the `saved` ref is bumped synchronously before the await so no double-write is possible, and that `0 / 0` cannot appear in the repair counter.
Task 11: complete (commits bd04a4b..fecaa05, review clean)
ALL 12 TASKS COMPLETE. Proceeding to the final whole-branch review.

FINAL REVIEW (opus, whole branch, 28 commits): not mergeable as-is. No Critical. Six Important must be fixed: (1) quitting during repair discards the whole round's answers and the streak day — persistence only fires at summary and there is no AppState listener; (2) an empty round still bumps the streak and renders "0 de 0 correctas"; (3) listening questions are silent in repair, because Task 10's auto-speak guard excluded 'repairing' and Task 11 later rendered repair through the same branch — invisible to both task reviews; (4) corrupt storage silently returns the 10-word seed, presenting as a fresh install; (5) app.json still ships name/slug "app"; (6) README documents the pre-workspace paths. Reviewer confirmed the repair-never-records invariant holds, and verified purity by scanning all 28 commit trees rather than trusting the guard test.
FINAL REVIEW: reviewer promoted deferred minor T6 (corrupt-blob) to Important, arguing the four-line backup fix does not depend on the phase-3 migration. I agree and am reversing my earlier deferral.
FINAL REVIEW: reviewer reviewed the three unreviewed items properly. `make_icon.py` judged sound and worth keeping (validates sources, asserts iOS opacity and Android alpha before claiming success) but not wired into package.json scripts. `pickOne` is pure and injection-respecting but its `!` lies about the empty-array case. `Welcome` is correct; its Math.random is the only one on the branch and wants a comment.
FINAL FIX WAVE: all 6 findings ADDRESSED, re-reviewed, no new breakage. Implementer verified FIX 1 by inspecting AsyncStorage directly (19→29 entries after quitting mid-repair, all ten answers present including the two misses) rather than via the home screen, whose tiles legitimately cannot move on the same day.
FINAL: Ruling (parked): if `saveProgress` throws inside `persistAnswers`, `persisted.current` was already claimed before the await, so the failed write is never retried and the streak bump after it never runs. Narrow — it needs an AsyncStorage failure — and the alternative (claiming after the await) reopens the double-write the counter exists to prevent. Parked for phase 3's storage migration, which is where a retry belongs. Cost if wrong: one round lost on a device with failing storage, which has larger problems.
FINAL: Ruling (parked): the corrupt-blob fix is a silent backup, not a recovery flow — the learner still sees what looks like a fresh install, with the old data preserved under a timestamped key. Surfacing it needs UI that phase 2 has not built. Cost if wrong: a learner who never learns their data was recoverable.
